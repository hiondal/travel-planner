# 이벤트 스토밍 리뷰 반영 결과

> 작성자: 아키 (소프트웨어 아키텍트)
> 작성일: 2026-02-22
> 목적: 8명의 팀원 리뷰 의견 반영 내역을 정리하고, MVP 이후로 미룬 항목의 사유를 명시한다.

---

## 1. 반영 완료 항목

### 1-1. 반드시 반영 (시퀀스 다이어그램 수정)

| # | 개선 항목 | 수정 파일 | 반영 내용 |
|:-:|----------|----------|----------|
| C1 | 무료/유료 티어 분기 정책 | `05-출발전브리핑생성.puml`, `07-대안장소검색.puml` | 브리핑 생성 시 구독 티어 확인 분기 추가 (Free: 1일 1회 제한, 초과 시 스킵). 대안 검색 시 Free 사용자 Paywall 표시 분기 추가 |
| C2 | 대안 카드 미선택/거절 분기 | `08-대안카드선택및일정반영.puml` | alt/else 분기로 "대안 미선택" 시 기존 일정 유지 흐름 추가. 미채택 사유(닫기/시간초과) 기록 이벤트 추가 |
| C4 | 대안 카드 3장 미만 예외 처리 | `07-대안장소검색.puml` | 초록 상태 후보 3장 미만 시 반경 단계적 확장 폴백 (1km -> 2km -> 3km) alt 분기 추가 |
| C7 | 장소 검색 결과에서 여행자 선택 인터랙션 | `01-여행일정등록.puml` | 장소 검색 결과 목록 제시 -> 여행자 선택 -> 상세 정보 조회 2단계 인터랙션으로 분리 |
| C8 | 모니터링 등록 통신 방식 불일치 수정 | `08-대안카드선택및일정반영.puml` | 동기 호출(상태판정업무 직접 호출)을 비동기 이벤트 발행(일정업무에서 모니터링 대상 변경 이벤트 발행)으로 변경. 컨텍스트 맵(5-2) 통신 방식과 일치 |
| C10 | Google Popular Times 명시 변경 | `02-외부데이터수집.puml`, `event-storming-결과.md` | 별도 "혼잡도 API" participant 제거. Google Places API의 popular_times 필드로 통합. 외부 시스템 의존성 테이블에서 혼잡도 API 행 삭제, Google Places API 용도에 혼잡도 추가 |

### 1-2. 정책/규칙 추가 (시퀀스 note + 결과 문서 P15~P22)

| # | 개선 항목 | 수정 파일 | 반영 내용 |
|:-:|----------|----------|----------|
| C5 | 위치정보 수집 동의 + Push 알림 권한 | `01-여행일정등록.puml`, `event-storming-결과.md` | 첫 일정 등록 시 위치정보 수집 동의 + Push 알림 권한 확인 alt 분기 추가. 정책 P15로 등록 |
| C6 | 분석용 이벤트 4개 추가 | `04-상태배지조회.puml`, `06-출발전브리핑수신.puml`, `07-대안장소검색.puml`, `08-대안카드선택및일정반영.puml` | 배지상세조회됨, 브리핑열람됨, 대안카드노출됨, 대안카드채택됨 4개 분석 이벤트를 각 시퀀스에 추가. 정책 P18로 등록 |
| C9 | 브리핑 멱등성 + 연속 방문 트리거 조정 | `05-출발전브리핑생성.puml`, `event-storming-결과.md` | 동일 장소+출발시간 브리핑 1회 제한, 연속 방문 간격 60분 미만 시 트리거 병합 정책 note 추가. 정책 P21로 등록 |
| C11 | Timezone 처리 정책 | `01-여행일정등록.puml`, `event-storming-결과.md` | 방문 일시 현지 시간(IANA) 기준 저장, 영업시간 검증 현지 시간 기준, 클라이언트 표시 사용자 설정 시간대 기준 정책 note 추가. 정책 P16로 등록 |
| C14 | 색약 사용자 접근성 (WCAG 2.1) | `04-상태배지조회.puml`, `event-storming-결과.md` | 색상 + 아이콘(체크/느낌표/X/물음표) 조합 접근성 정책 note 추가. 정책 P17로 등록 |
| C15 | 외부 API 부분 실패 처리 | `02-외부데이터수집.puml`, `event-storming-결과.md` | 모든 API 성공/일부 실패/전체 실패 3단계 alt 분기 추가. 실패 항목별 개별 캐시 fallback, 품질 메타데이터(is_cached, data_age_seconds) 첨부. 정책 P20으로 등록 |

### 1-3. 결과 문서 보완 (섹션 추가)

| # | 개선 항목 | 반영 내용 |
|:-:|----------|----------|
| M1 | 인증/구독 최소 요구사항 | `event-storming-결과.md` 섹션 9 추가. 구독 티어(Free/Trip Pass/Pro) 정의, 티어 확인 지점 2곳 명시, MVP 인증 최소 요구사항(소셜 로그인, JWT) 정리 |
| D6 | MVP 배포 전략 | `event-storming-결과.md` 섹션 10 추가. 모놀리스 우선 전략(v0) -> 모니터링 분리(v1) -> 점진적 분리(v2+) 3단계 로드맵. 논리적 모듈 경계 유지 원칙 명시 |

---

## 2. MVP 이후로 미룬 항목

### 2-1. 우선순위 1 중 미룬 항목

| # | 개선 항목 | 미룬 사유 | 반영 시점 |
|:-:|----------|----------|----------|
| C3 | 일정 반영 되돌리기(Undo) | UX 설계(토스트 3~5초 표시) 및 상태 롤백 로직이 복잡. MVP 핵심 흐름에는 영향 없음 | 유저스토리 단계에서 상세 설계 |

### 2-2. 우선순위 2 (설계 단계 반영)

| # | 개선 항목 | 미룬 사유 | 반영 시점 |
|:-:|----------|----------|----------|
| C12 | 상태 배지 갱신 통신 방식 (SSE) | 프론트엔드 설계 단계에서 SSE + Visibility Change 하이브리드 상세 설계 필요 | 프론트엔드 설계 |
| C13 | Push 알림 딥링크 3케이스 분기 | 포어그라운드/백그라운드/콜드스타트 분기는 프론트엔드 구현 상세 | 프론트엔드 설계 |
| C16 | FCM 발송 실패 재시도 + fallback | 인프라/운영 레벨 설계 항목. 최대 2회 재시도, 인앱 알림 fallback | 물리 아키텍처 설계 |
| C17 | 대안 카드 선택 실패 롤백 UI | 낙관적 업데이트 롤백은 프론트엔드 구현 상세 | 프론트엔드 설계 |
| C18 | 오프라인 모드 대응 정책 | 전체 아키텍처에 영향. 캐시 전략, 동기화 로직 필요 | 아키텍처 패턴 선정 |
| C19 | 상태 배지 조회 경로 통일 | UX-시스템 정합성 검토 필요. 기능 영향 없음 | 유저스토리 단계 |
| C20 | 브리핑 생성 캐시 전략 | Read-Through 방식 권장. API 설계 단계에서 캐시 레이어 상세 설계 | API 설계 |

### 2-3. 우선순위 3 (유저스토리 단계 보완)

| # | 개선 항목 | 비고 |
|:-:|----------|------|
| C21 | 브리핑 무시/나중에 보기 | 브리핑 히스토리 재진입 경로 + 유효 시간 정책 |
| C22 | 알림 설정/수신 거부(DND) | 알림 빈도/시간대 제어, 수면 시간 차단 |
| C23 | 여행 완료/피드백 수집 | NPS, 정확도 피드백, Accuracy Flywheel |
| C24 | 온보딩/첫 사용 시나리오 | 배지 메타포 안내, Push 권한 획득 타이밍 |
| C25 | 일정 수정/삭제 시나리오 | 장소 삭제 시 모니터링 해제, 이동시간 재계산 |
| C26 | 상태 회복(역전이) 알림 정책 | 빨강->초록 회복 시 긍정적 알림 여부 |
| C27 | 공휴일/임시 휴무 처리 | 현지 공휴일 DB 연동 |
| C28 | 여행 종료 후 모니터링 자동 해제 | 여행 기간 종료 시 수집 중단 정책 |

### 2-4. AI 고도화 준비 (마법사 의견)

| # | 항목 | 반영 시점 |
|:-:|------|----------|
| A1 | 상태 판정 신뢰도 점수(Confidence Score) | AI 모델 고도화 시 |
| A2 | 수집 데이터 품질 메타데이터 | **C15 반영 시 일부 적용** (is_cached, data_age_seconds) |
| A3 | 사용자 반응 피드백 루프 이벤트 | **C6 반영 시 일부 적용** (브리핑열람됨, 대안카드채택됨) |
| A4 | 대안 카드 비선택 사유 수집 | **C2 반영 시 일부 적용** (미채택 사유 기록) |
| A5 | 여행 스타일 프로파일 이벤트 | Phase 2+ (개인화) |
| A6 | 브리핑 텍스트 생성 단계 분리 | Phase 3 (AI 자동 재조정) |
| A7 | StatusHistory 보존 정책 | 데이터 설계 단계 |

### 2-5. DevOps 인프라 (파이프 의견)

| # | 항목 | 반영 시점 |
|:-:|------|----------|
| D1 | 헬스체크 Readiness/Liveness Probe | 물리 아키텍처 설계 |
| D2 | 환경별 설정 관리 | 물리 아키텍처 설계 |
| D3 | 분산 추적(Distributed Tracing) | 물리 아키텍처 설계 |
| D4 | 스케줄러 잡 Watchdog | 운영 설계 |
| D5 | 데이터 보존 정책/스토리지 계획 | 데이터 설계 |
| D6 | MVP 모놀리스 우선 접근 | **결과 문서 섹션 10에 반영 완료** |

---

## 3. 수정 파일 목록

| 파일 | 수정 항목 |
|------|----------|
| `01-여행일정등록.puml` | C7(장소 선택 인터랙션), C5(위치정보/Push 동의), C11(Timezone 정책) |
| `02-외부데이터수집.puml` | C10(Google Places API 통합), C15(부분 실패 처리) |
| `04-상태배지조회.puml` | C6(배지 상세 조회됨 분석이벤트), C14(접근성 정책) |
| `05-출발전브리핑생성.puml` | C1(무료/유료 분기), C9(멱등성 정책) |
| `06-출발전브리핑수신.puml` | C6(브리핑 열람됨 분석이벤트) |
| `07-대안장소검색.puml` | C1(Paywall 분기), C4(반경 확장 폴백), C6(대안 카드 노출됨 분석이벤트) |
| `08-대안카드선택및일정반영.puml` | C2(미선택 분기), C8(비동기 이벤트 발행), C6(대안 카드 채택됨 분석이벤트) |
| `event-storming-결과.md` | P15~P22 추가, 외부 시스템 수정, 이벤트 클러스터링 갱신, 통신 방식 보완, 섹션 9(인증/구독), 섹션 10(배포 전략) |

---

## 4. 추가된 정책/규칙 요약

| # | 정책/규칙 | 근거 |
|:-:|----------|------|
| P15 | 위치정보 수집 동의 + Push 알림 권한 (위치정보법/GDPR) | C5 (인사이트) |
| P16 | Timezone: 현지 시간(IANA) 기준 저장/검증 | C11 (인사이트) |
| P17 | 배지 접근성: 색상 + 아이콘 조합 (WCAG 2.1) | C14 (데브-프론트) |
| P18 | 분석 이벤트 최소 4개 수집 | C6 (피오, 마법사) |
| P19 | 무료/유료 티어 분기 (Free: 브리핑 1일 1회, 대안 미제공) | C1 (피오) |
| P20 | 외부 API 부분 실패 시 개별 캐시 fallback | C15 (가디언) |
| P21 | 브리핑 멱등성 + 연속 방문 60분 미만 트리거 병합 | C9 (인사이트, 데브-백, 가디언) |
| P22 | 대안 3장 미만 시 반경 확장 (1km->2km->3km) | C4 (도그냥, 인사이트, 데브-백, 가디언) |
